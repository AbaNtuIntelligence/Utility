# docker-compose.yml

version: '3.9'

services:
  web:
    build: .
    command: gunicorn config.asgi:application
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  db:
    image: postgres:15

  redis:
    image: redis:7